pipeline {
    agent any

    stages {
        stage('Build app') {
            steps {
                echo "hello"
            }
        }

        stage('Build docker image') {
            steps {
                echo "world"
            }
        }
    }
}
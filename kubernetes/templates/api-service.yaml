apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.spring.apiServiceName }}
spec:
  ports:
    - port: 80
      targetPort: {{ .Values.spring.port }}
  selector:
    app: {{ .Values.spring.apiServiceName }}
  type: ClusterIP
